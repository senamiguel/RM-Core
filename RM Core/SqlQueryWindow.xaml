<Window x:Class="RM_Core.SqlQueryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
        ui:WindowHelper.UseModernWindowStyle="True"
        ui:WindowHelper.SystemBackdropType="Mica"
        ui:TitleBar.Height="36"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        mc:Ignorable="d"
        Title="Executar SQL"
        Height="500" Width="700"
        Background="#1C1C1E"
        FontFamily="Inter, Segoe UI, Arial"
        FontSize="13">
  <Window.Resources>
    <Style x:Key="PrimaryBtnStyle" TargetType="Button">
      <Setter Property="Background" Value="#0078D4"/>
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="Height" Value="34"/>
      <Setter Property="Width" Value="100"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" SnapsToDevicePixels="True">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="border" Property="Background" Value="#0086F0"/>
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="border" Property="Background" Value="#005A9E"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>

  <Grid Margin="15">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <Border Grid.Row="0" Background="#0CFFFFFF" BorderBrush="#1AFFFFFF" BorderThickness="1" CornerRadius="8" Padding="12,8" Margin="0,0,0,10">
      <TextBlock x:Name="txtDbInfo" Text="Servidor: - | Base: - | Tipo: -" FontSize="13" Foreground="#80FFFFFF" VerticalAlignment="Center"/>
    </Border>

    <TextBlock Grid.Row="1" Text="SQL Query" FontSize="11" FontWeight="SemiBold" Foreground="#80FFFFFF" Margin="0,0,0,6"/>

    <Border Grid.Row="2" Background="#0A0A0B" BorderBrush="#1AFFFFFF" BorderThickness="1" CornerRadius="8" Padding="4" Margin="0,0,0,10">
      <TextBox x:Name="txtSqlQuery" Height="120" Background="Transparent" BorderThickness="0" Foreground="#E6FFFFFF" FontFamily="Consolas, monospace" FontSize="12" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" CaretBrush="White" Padding="8,6"/>
    </Border>

    <Grid Grid.Row="3">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <Grid Grid.Row="0" Margin="0,0,0,8">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="12" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Button x:Name="btnExecutar" Grid.Column="0" Content="Executar" Style="{StaticResource PrimaryBtnStyle}" Click="btnExecutar_Click"/>
        <TextBlock x:Name="txtStatus" Grid.Column="2" Text="" FontSize="12" Foreground="#80FFFFFF" VerticalAlignment="Center"/>
      </Grid>

      <Border Grid.Row="1" Background="#0A0A0B" BorderBrush="#1AFFFFFF" BorderThickness="1" CornerRadius="8" Padding="0" ClipToBounds="True">
        <DataGrid x:Name="gridResultados" AutoGenerateColumns="True" IsReadOnly="True" Background="#0A0A0B" Foreground="White" BorderThickness="0" RowBackground="Transparent" AlternatingRowBackground="#0A1111" GridLinesVisibility="None" HeadersVisibility="Column" FontFamily="Consolas, monospace" FontSize="11" Margin="0">
          <DataGrid.Resources>
            <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#1C1C1E"/>
          </DataGrid.Resources>
        </DataGrid>
      </Border>
    </Grid>
  </Grid>
</Window>
